class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        # read the first few indexes e.g, if index[0] = different index[0]:
        # output the first letter, and etc.
        print(strs)

        # if there is no longest common prefix, return ""
        # else:
        #     return ""